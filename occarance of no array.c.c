#include <stdio.h>
 int main()
{
	int arr[40];
	int n,i=0,count;
	
	//accept array
	printf("enter the number-");
	scanf("%d",&n);
	
	for(i=0;i<n;i++)
	{
		if(arr[i==n])
		{
			count++;
		}
}
  {
  	printf("the occarance of %d is=%d",n,count);
  }
}
	
	
