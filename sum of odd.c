#include <stdio.h>
  int main()
{
	int a[10],i,n,sum=0,even,odd;
	
	printf("enter the number-");
	scanf("%d",&n);
	
	for(i=0;i<n;i++);
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++);
	{
		if(a[i]%2==1)
		even=sum+a[i];
	}
	      if	
	;{
		odd=sum+a[i];
	}
	printf("total sum of even value is-%d",even);
	printf("total sum of odd value is-%d",odd);	
}
