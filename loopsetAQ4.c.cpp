#include <stdio.h>
  int main()
{
	int a;
	 printf("Enter the integer");
	 scanf("%d",&a);
	 
	 for(i=1,i<=a,i++)
	 {
	 	printf("it is a prime number");
	 }
	 else
	 {
	 	printf("it is not a prime number");
	 }
}
